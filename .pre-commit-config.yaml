repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff
        args: ["--fix"]
        exclude: migrations/versions/*
      - id: ruff-format
        args: ["--fix-only"]
